version: 2.1
jobs:
  install:
    working_directory: ~/project
    docker:
      - image: vuejs/ci
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            ls -al
            yarn install
            ls -al
            yarn lint

workflows:
  version: 2
  install_and_lint:
    jobs:
      - install